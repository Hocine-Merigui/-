"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, CircleCheckBig, ClipboardList, Clock3, Plus, Search, TrendingUp, Users, WalletCards } from "lucide-react";
import { store } from "@/lib/store";
import type { Customer, Order } from "@/lib/types";
import StatusBadge from "@/components/status-badge";
export default function DashboardPage(){const [customers,setCustomers]=useState<Customer[]>([]);const [orders,setOrders]=useState<Order[]>([]);const [q,setQ]=useState("");useEffect(()=>{setCustomers(store.customers());setOrders(store.orders())},[]);const filtered=useMemo(()=>orders.filter(o=>`${o.customerName} ${o.phone} ${o.orderNumber} ${o.serviceTitle}`.toLowerCase().includes(q.toLowerCase())),[orders,q]);const total=orders.reduce((s,o)=>s+o.paid,0);const done=orders.filter(o=>o.status==="مكتمل").length;const active=orders.filter(o=>!['مكتمل','ملغى'].includes(o.status)).length;const cards=[{label:"إجمالي الزبائن",value:customers.length,icon:Users},{label:"كل الطلبات",value:orders.length,icon:ClipboardList},{label:"قيد المتابعة",value:active,icon:Clock3},{label:"مكتملة",value:done,icon:CircleCheckBig}];return <div className="mx-auto max-w-7xl animate-enter"><header className="mb-7 flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-black text-blue-600">HOCINE TECH+</p><h1 className="mt-1 text-3xl font-black tracking-tight text-slate-950">لوحة التحكم</h1><p className="mt-2 text-sm font-medium text-slate-400">ملخص سريع للعمل والطلبات التي تحتاج إلى متابعة.</p></div><div className="flex gap-2"><Link href="/dashboard/customers" className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-black text-slate-700 shadow-sm">زبون جديد</Link><Link href="/dashboard/orders/new" className="flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-black text-white shadow-lg"><Plus size={17}/> طلب جديد</Link></div></header><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({label,value,icon:Icon})=><article key={label} className="card-hover rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-5 flex items-center justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={20}/></div><span className="text-[10px] font-black text-slate-300">مباشر</span></div><p className="text-sm font-bold text-slate-500">{label}</p><p className="mt-2 text-3xl font-black text-slate-950">{value}</p></article>)}</section><div className="mt-6 grid gap-6 xl:grid-cols-[1fr_330px]"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><div className="relative"><Search size={18} className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث بالاسم، الهاتف، رقم الطلب أو الخدمة..." className="h-12 w-full rounded-xl border border-slate-200 bg-slate-50 pr-11 pl-4 text-sm outline-none focus:border-blue-400 focus:bg-white focus:ring-4 focus:ring-blue-50"/></div></div><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div><h2 className="font-black text-slate-900">آخر الطلبات</h2><p className="mt-1 text-xs text-slate-400">نتائج مطابقة: {filtered.length}</p></div><Link href="/dashboard/orders" className="flex items-center gap-1 text-xs font-black text-blue-600">جميع الطلبات <ArrowLeft size={15}/></Link></div><div className="divide-y divide-slate-100">{filtered.slice(0,7).map(o=><Link key={o.id} href="/dashboard/orders" className="flex flex-col gap-3 p-5 transition hover:bg-slate-50 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-black text-slate-800">{o.customerName}</h3><span className="text-[11px] font-bold text-slate-400">{o.orderNumber}</span></div><p className="mt-1 text-sm font-semibold text-slate-500">{o.serviceTitle}</p><p className="mt-1 text-xs text-slate-400">{o.phone}</p></div><div className="flex items-center gap-4"><div className="text-left"><p className="text-[10px] font-bold text-slate-400">المتبقي</p><p className="mt-1 text-sm font-black text-slate-800">{(o.price-o.paid).toLocaleString()} دج</p></div><StatusBadge status={o.status}/></div></Link>)}{filtered.length===0&&<div className="p-10 text-center text-sm font-bold text-slate-400">لا توجد نتائج مطابقة.</div>}</div></section><aside className="space-y-5"><section className="rounded-2xl bg-slate-950 p-6 text-white shadow-xl shadow-slate-200"><div className="mb-5 grid h-11 w-11 place-items-center rounded-xl bg-white/10"><WalletCards size={20}/></div><p className="text-sm font-bold text-slate-400">إجمالي المبالغ المسجلة</p><p className="mt-2 text-3xl font-black">{total.toLocaleString()} دج</p><p className="mt-5 flex items-center gap-2 text-xs font-black text-emerald-400"><TrendingUp size={16}/> يتم احتسابها من الطلبات</p></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black text-slate-900">تنبيه العمل</h3><div className="mt-4 space-y-3"><div className="rounded-xl bg-amber-50 p-4"><p className="text-sm font-black text-amber-800">{orders.filter(o=>o.status==="في انتظار الوثائق").length} طلب</p><p className="mt-1 text-xs font-semibold text-amber-600">في انتظار الوثائق</p></div><div className="rounded-xl bg-blue-50 p-4"><p className="text-sm font-black text-blue-800">{orders.filter(o=>o.status==="قيد الإنجاز").length} طلب</p><p className="mt-1 text-xs font-semibold text-blue-600">قيد الإنجاز حاليًا</p></div></div></section></aside></div></div>}
