"use client";
import { useEffect, useMemo, useState } from "react";
import { Banknote, CircleDollarSign, CreditCard, WalletCards } from "lucide-react";
import { store } from "@/lib/store";
import type { Order } from "@/lib/types";
export default function PaymentsPage(){const [orders,setOrders]=useState<Order[]>([]);useEffect(()=>setOrders(store.orders()),[]);const stats=useMemo(()=>({total:orders.reduce((s,o)=>s+o.price,0),paid:orders.reduce((s,o)=>s+o.paid,0),remaining:orders.reduce((s,o)=>s+Math.max(o.price-o.paid,0),0),unpaid:orders.filter(o=>o.price>o.paid).length}),[orders]);const cards=[{label:"قيمة الخدمات",value:stats.total,icon:CircleDollarSign},{label:"المبالغ المدفوعة",value:stats.paid,icon:WalletCards},{label:"المبالغ المتبقية",value:stats.remaining,icon:Banknote},{label:"طلبات غير مسددة",value:stats.unpaid,icon:CreditCard,noCurrency:true}];return <div className="mx-auto max-w-7xl animate-enter"><header className="mb-6"><p className="text-sm font-black text-blue-600">الحسابات</p><h1 className="mt-1 text-3xl font-black text-slate-950">المدفوعات</h1><p className="mt-2 text-sm font-medium text-slate-400">نظرة مالية سريعة مبنية على الطلبات المسجلة.</p></header><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({label,value,icon:Icon,noCurrency})=><article key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-50 text-emerald-600"><Icon size={20}/></div><p className="mt-5 text-sm font-bold text-slate-500">{label}</p><p className="mt-2 text-2xl font-black text-slate-950">{value.toLocaleString()} {!noCurrency&&"دج"}</p></article>)}</section><section className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="font-black text-slate-900">تفاصيل الطلبات</h2><p className="mt-1 text-xs font-semibold text-slate-400">الطلبات التي لديها مبالغ متبقية تظهر أولًا.</p></div><div className="divide-y divide-slate-100">{[...orders].sort((a,b)=>(b.price-b.paid)-(a.price-a.paid)).map(o=><div key={o.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-black text-blue-600">{o.orderNumber}</p><h3 className="mt-1 font-black text-slate-800">{o.customerName}</h3><p className="mt-1 text-xs font-semibold text-slate-400">{o.serviceTitle}</p></div><div className="grid grid-cols-3 gap-5 text-center text-xs"><div><p className="font-bold text-slate-400">السعر</p><p className="mt-1 font-black text-slate-800">{o.price.toLocaleString()}</p></div><div><p className="font-bold text-slate-400">مدفوع</p><p className="mt-1 font-black text-emerald-600">{o.paid.toLocaleString()}</p></div><div><p className="font-bold text-slate-400">متبقي</p><p className="mt-1 font-black text-rose-600">{Math.max(o.price-o.paid,0).toLocaleString()}</p></div></div></div>)}</div></section></div>}
